version: '3.4'

services:
  new-api:
    image: calciumion/new-api:1.0.0-alpha.1
    container_name: genbo-api
    restart: always
    command: --log-dir /app/logs
    ports:
      - "30000:3000"
    volumes:
      - ./data:/data
      - ./logs:/app/logs
    environment:
      - SQL_DSN=root:NWtxDHTT5hNrzcWi@tcp(127.0.0.1:13306)/new_api?parseTime=true  # Point to the mysql service
      - REDIS_CONN_STRING=redis://:jFmyXHkLPYZTspNA@127.0.0.1:26739/0
      - TZ=Asia/Shanghai
    #      - SESSION_SECRET=random_string  # 多机部署时设置，必须修改这个随机字符串！！！！！！！
    #      - NODE_TYPE=slave  # Uncomment for slave node in multi-node deployment
    #      - SYNC_FREQUENCY=60  # Uncomment if regular database syncing is needed
    #      - FRONTEND_BASE_URL=https://openai.justsong.cn  # Uncomment for multi-node deployment with front-end URL

    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:3000/api/status | grep -o '\"success\":\\s*true' | awk -F: '{print $$2}'"]
      interval: 30s
      timeout: 10s
      retries: 3
